# FriMe 需要検証LP 実装依頼

あなたはシニアWebフロントエンドエンジニア兼UI実装者です。以下の仕様で、需要検証用のランディングページを実装してください。

**このLPの目的はサービスの提供ではありません。「こういうサービスを検討しているが、どう思うか」を聞くアンケートに回答してもらうことだけが目的です。** 過剰な機能開発・バックエンド・認証・DBは一切作らないでください。

導線は `X（旧Twitter） → LP → Googleフォーム` のみです。

---

## 1. 最重要ルール（違反禁止）

- **実績を捏造しない。** 架空の口コミ、利用者数、メディア掲載、星評価、「〇〇人が登録」などを一切書かない。サービスはまだ存在しない
- **「安全を完全に保証する」と書かない。** 「スクリーンショット完全防止」など技術的に保証できない表現も禁止
- **開催日・募集人数を書かない。** 現時点で未定。「開催が決まったら案内する」という表現に留める
- **CTAは「アンケートに答える」。** 「登録する」「今すぐ始める」「参加する」「ダウンロード」は使わない
- **顔出しと週末に会うことを、ファーストビュー内に明示する。** 下の方に隠さない。ここを隠すとアンケート結果が無意味になる
- **「マッチング」より「会う」を強調する。** ただし不安を煽らない
- 正直であることそのものを信頼感に変えるコピーにする

---

## 2. サービス概要（LPで伝える内容）

コンセプト:

> **金曜21時、15分だけ話す。気が合えば、その週末に会う。**

一般的なマッチングアプリと違い、ユーザーは相手を探さない・スワイプしない・長いメッセージをしない。

想定フロー:

1. 参加を申し込む
2. 金曜21:00、運営/AIが選んだ相手と15分だけ顔を見ながら話す（1晩に最大3人）
3. 15分でタイマー終了。直後に各自が回答（相手の回答は見えない）
   - 会いたい ／ もう15分話す ／ やめておく
4. 両者が「もう15分話す」またはどちらかが「会いたい」の場合、その場で通話を15分延長（日を改めない）
5. 2人とも「会いたい」だったときだけ次へ進む
6. AIが週末のデートプランを提案
7. **翌日か翌々日**の中で、時間も場所も2人の合意で決めて実際に会う

---

## 3. デザインシステム（厳守）

Refero の「MindMarket」スタイルに準拠する。以下の値をそのまま使うこと。**URLは参照しない。以下の定義が唯一の正とする。**

### カラー

```css
--cream: #f5f1e4; /* ページ全体の背景。#ffffff を背景に使わない */
--white: #ffffff; /* 浮いたカード・ナビのみ */
--sandstone: #e0dbce; /* 沈んだ面 */
--ink: #2c2e2a; /* 本文・見出し・アイコン。#000000 は使わない */
--gray: #80827f; /* 補助テキスト */
--hairline: #d5d5d4; /* 極薄の罫線 */
--green: #8ed462; /* 構造アクセント専用。文字色・大面積に使わない */
--blue: #2ba0ff; /* 装飾・小さな丸 */
--coral: #ff705d; /* アクション（CTA）専用 */
--yellow: #f5e211; /* フッター帯・装飾のみ */
```

### タイポグラフィ

- **Inter 一本。** 見出し用に別フォントを入れない。日本語は `Inter` + `"Noto Sans JP"` のフォールバックでよい
- ウェイトは **400 と 500 のみ**。600以上を使わない
- 見出し: `line-height: 0.98`、`letter-spacing: -0.05em`
- 本文: 15〜18px、`line-height: 1.5〜1.6`、字送りは標準

### 形

- **角丸は 50px を基本とする。** カード・ボタン・ナビすべて。小さなチップのみ 10px
- **影・グラデーションを一切使わない。** クリーム地と白カードの色差だけで階層を作る
- ナビは矩形ヘッダーではなく、**白い1本のピル**を浮かせる
- CTAボタンは**塗り + 内側の小さな丸**で構成する（コーラル地に白い丸）
- 1つのパーツに2色の彩色を入れない

### イラスト

- 写真・ストックフォト・3D・グラデーションは使わない
- **SVGの単純な図形（丸・角丸長方形）だけで構成する。** 外部画像を読み込まない
- 現時点でイラストレーターへの発注は行わないため、図形のみで成立させること

---

## 4. レスポンシブ（前回崩れた箇所。特に注意）

**Xからの流入が主なので、スマホを最優先に実装すること。**

- 見出しは固定pxではなく `clamp()` で指定する
  - 例: `font-size: clamp(34px, 8.5vw, 58px);`
  - 日本語の全角は約1em幅。「金曜21時、15分だけ話す。」は約11.6em相当なので、横幅からはみ出さない係数を選ぶこと
- **丸い要素（アバター、CTA内の丸、箇条書きのドット）には必ず `flex-shrink: 0` を付ける。** 付けないと潰れて楕円になる
- スマホでは見出しを2行×2ブロックに改行する（`金曜21時、／15分だけ話す。` `気が合えば、／その週末に会う。`）
- PCでは見出しを全幅1行×2ブロックにする。2カラムの片側に巨大見出しを入れない
- スマホのCTAは全幅ボタンにする（角丸50pxは維持）
- 375px / 768px / 1200px で確認すること

---

## 5. ファーストビュー（最重要）

上から順に:

1. 白いピルナビ（左: FriMe ／ 右: 「アンケート」ボタン + 緑の丸いメニューボタン）
2. 見出し
   - 1行目ブロック（ink、weight 500）: `金曜21時、15分だけ話す。`
   - 2行目ブロック（gray、weight 400）: `気が合えば、その週末に会う。`
3. 説明文: `相手を探さない。長いメッセージも要らない。まず15分だけ話して、2人とも「会いたい」なら、翌日か翌々日に会います。`
4. **条件3行**（各行の先頭に小さな色付きの丸。緑・青・コーラルの順）
   - `15分、顔を見ながら話します`
   - `2人ともYESのときだけ、次に進みます`
   - `まだ何も作っていません`
5. CTA（コーラル、内側に白い丸）: `アンケートに答える`
6. CTA下の注記: `約2分／開催日(リリース日)はまだ決まっていません`
7. 折り返し位置に、白カードが下から覗く（`border-radius: 44px 44px 0 0`）

**4番をファーストビューから外さないこと。** 摩擦を隠して集めた登録は検証の役に立たない。

---

## 6. セクション構成

`components/` 配下に分割する。過剰な抽象化は禁止。

| #   | コンポーネント   | 内容                                                                                                                                                                                                                                                                                                                                    |
| --- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `Nav`            | 白いピルナビ。スクロールで固定しなくてよい                                                                                                                                                                                                                                                                                              |
| 2   | `Hero`           | 上記のファーストビュー                                                                                                                                                                                                                                                                                                                  |
| 3   | `Problem`        | 見出し `マッチングしても、なかなか会わない。` ／ 5項目をカードで。煽らず「これ、分かる」の温度感。<br>・マッチしたけどメッセージが続かない<br>・何日もやり取りして結局会わない<br>・会う前に相手がどんな人か分からない<br>・初デートに誘うタイミングが難しい<br>・実際に会うのが少し不安                                                |
| 4   | `Solution`       | 見出し `だから、最初から「会う」ことをゴールにする。` ／ ①〜⑥のステップを白いカードで縦に。スクロールで順に現れる                                                                                                                                                                                                                       |
| 5   | `MutualYes`      | 見出し `「会いたい」は、2人ともYESのときだけ。` ／ 3つの選択肢（会いたい／もう15分話す／やめておく）を縦のピルで表示。「会いたい」だけコーラル。**相手の回答は見えないこと**を明記。**女性の不安に最も効くセクションなので、Problemの直後に近い位置に置く**                                                                             |
| 6   | `FifteenMinutes` | 見出し `まず15分。` ／ タイマーUI（`15:00 → 08:32 → 00:00`）はCSSアニメーションの演出のみ。実機能不要                                                                                                                                                                                                                                   |
| 7   | `Weekend`        | 見出し `翌日か翌々日で、2人で決める。` ／ 「土 午前／土 午後／土 夜／日 午前／日 午後／日 夜」のチップを並べ、1つだけ緑。下に `時間も場所も、2人の合意で決まります。`                                                                                                                                                                   |
| 8   | `AiPlan`         | 見出し `会うことが決まったら、AIが初デートを考える。` ／ サンプルプランを白カードで（例: 14:00 渋谷駅集合 → 14:15 静かなカフェ → 15:30 展示 → 17:00 解散）。**「AIが決める」ではなく「AIが2人のデートを考える」**というニュアンスにする。予約不要の場所を中心に提案する旨も書く                                                         |
| 9   | `Safety`         | 見出し `安心して、最初の15分を。` ／ 以下を簡潔に。**すべて「予定」「検討中」と明示する**<br>・通話の録画機能は実装しない予定です<br>・相手の回答内容はお互いに公開しません<br>・通報・ブロックなどの安全機能を予定しています<br>・年齢確認・本人確認を含む安全設計を検討しています<br>・実証段階では運営が手動で進行します             |
| 10  | `Maker`          | **一人称のメッセージ。** 白カード1枚に以下を配置。架空の肩書きや写真は使わない<br>`このサービスは、まだ何もありません。マッチングアプリで100人とマッチして誰にも会えなかった、という話をよく聞きます。なぜそうなるのかを考えて、この形にたどり着きました。作る前に、これが本当に欲しいものなのか知りたいので、意見を聞かせてください。` |
| 11  | `Cta`            | 大きめのCTAブロック。文言・注記はHeroと同一                                                                                                                                                                                                                                                                                             |
| 12  | `Faq`            | 下記                                                                                                                                                                                                                                                                                                                                    |
| 13  | `Footer`         | 黄色(`--yellow`)の帯。FriMe（仮称）／ Copyright ／ `本ページは、開発前のサービスに関する意見募集を目的としたページです。` ／ 問い合わせ先を後から入れられる空欄。**利用規約・プライバシーポリシーの本文は作らない**                                                                                                                     |

CTAはHero・中盤・末尾の最低3箇所に置く。すべて同じGoogleフォームへ遷移する。

---

## 7. FAQ

```
Q. これはもう使えるサービスですか？
A. いいえ。まだ何も作っていません。作る前に意見を聞いている段階です。

Q. 今回は何をするのですか？
A. 2分ほどのアンケートに答えていただくだけです。それ以上のことはありません。

Q. いつ始まりますか？
A. 決まっていません。第1回の開催が決まったら、ご希望の方にご案内します。

Q. 顔出しは必要ですか？
A. オンラインデートのため、顔を見ながら話すことを想定しています。

Q. 合わなかった場合は？
A. 「やめておく」を選べます。あなたの回答内容は相手に公開しません。

Q. 誰でも参加できますか？
A. 年齢確認・本人確認を含む安全設計を検討しています。参加条件は開催時にご案内します。
```

---

## 8. GoogleフォームURLとCTA計測

```ts
// lib/config.ts
export const GOOGLE_FORM_URL = "https://docs.google.com/forms/XXXX";

export const formUrl = (source: string) =>
  `${GOOGLE_FORM_URL}?usp=pp_url&utm_source=lp&utm_content=${source}`;
```

- **URLはこの1箇所だけを書き換えれば全CTAに反映されること**
- CTAごとに `source` を変える（`hero` / `mid` / `footer`）ことで、どのCTAから来たかをフォーム側で判別できるようにする
- 環境変数は使わない（GitHub Pagesで扱いづらいため）
- リンクは `<a href={formUrl("hero")} target="_blank" rel="noopener noreferrer">`

**独自のアクセス解析システムや個人情報収集の仕組みは作らないこと。**

---

## 9. 技術要件

必須:

- Next.js（App Router）／ TypeScript ／ Tailwind CSS
- `output: 'export'` による静的エクスポート
- GitHub Pages 対応 ／ GitHub Actions でのデプロイ

不要（作らない）:

- DB／認証／ログイン／ユーザー登録／決済／AI API／バックエンド／マッチング機能／ビデオ通話／管理画面

### GitHub Pages の設定（ここでハマりやすいので必ず対応）

`next.config.ts`:

```ts
const isProd = process.env.NODE_ENV === "production";
const repo = "REPO_NAME"; // ← リポジトリ名に合わせて1箇所だけ変更

export default {
  output: "export",
  basePath: isProd ? `/${repo}` : "",
  assetPrefix: isProd ? `/${repo}/` : "",
  images: { unoptimized: true },
  trailingSlash: true,
};
```

- **`public/.nojekyll` を必ず作成する**（無いと `_next/` 配下が配信されずCSSが当たらない）
- GitHub Actions で `actions/configure-pages` → `actions/upload-pages-artifact`（`path: ./out`）→ `actions/deploy-pages` の構成にする
- リポジトリの Settings → Pages で Source を「GitHub Actions」にする手順をREADMEに書く

---

## 10. アニメーション・パフォーマンス・アクセシビリティ

- アニメーションはCSS中心。スクロールでの軽いフェードイン、CTAのhover、タイマー数字の変化程度に留める
- `prefers-reduced-motion: reduce` に対応する
- 外部画像を読み込まない。イラストはインラインSVG
- semantic HTML ／ キーボード操作 ／ 十分なコントラスト（`--gray` を小さい文字に使いすぎない）

---

## 11. SEO / OGP

```
title:       金曜21時、15分だけ話す。気が合えば、その週末に会う。｜FriMe
description: マッチすることではなく、実際に会うことをゴールにした新しいデートサービスを検討しています。開発前の意見を募集中です。
```

- OGP画像は**SVGからPNGを手作業で用意せず**、クリーム地に見出しを置いた静的な `public/ogp.png` を仮で生成するか、無い場合はその旨をREADMEに書いて後から差し替えられるようにする
- favicon は単純な図形（クリーム地にコーラルの丸）でよい
- viewport 設定を忘れないこと

---

## 12. 実装手順

1. プロジェクト構成を確認し、必要な依存だけ追加する
2. `lib/config.ts` にGoogleフォームURLと色トークンを集約する
3. セクション単位でコンポーネントを実装する
4. **375px幅で崩れがないことを最優先で確認する**
5. `npm run build` と静的エクスポートが成功することを確認する
6. GitHub Actions のワークフローを作成する
7. README.md を書く

## 13. 完了条件

- `npm run build` が成功し、`out/` が生成される
- 375px / 768px / 1200px のいずれでも横スクロールが発生しない
- 見出しがどの幅でもコンテナからはみ出さない
- 丸い要素が潰れていない
- すべてのCTAが同じGoogleフォームへ遷移し、URLは1箇所の変更で全反映される
- OGP・favicon・viewport が設定されている
- バックエンド・APIキーが存在しない
- README に、GitHub Pages公開手順・GoogleフォームURLの変更方法・ファイル構成・使用技術が記載されている

---

## 14. 最後に

このLPを「マッチングアプリのLP」として作らないでください。

**「金曜の夜に15分話して、その週末に実際に誰かと会う、新しい週末の過ごし方」**を検討していることの告知として作ってください。

そして、このLPの成功はPVではありません。**「まだ何もできていないと分かった上で、それでも意見を書いてくれる人が集まること」**です。
